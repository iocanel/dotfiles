#!/bin/bash

 DISPLAY=:0 emacsclient -c -F '((name . "Floating Org Capture"))' --eval "(progn (switch-to-buffer (generate-new-buffer \"*capture*\")) (delete-other-windows) (add-hook 'org-select-mode-hook 'delete-other-windows) (org-capture nil))"
