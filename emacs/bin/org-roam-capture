#!/bin/bash

 DISPLAY=:0 emacsclient -c -F '((name . "Floating Org Roam Capture"))' --eval "(progn (switch-to-buffer (generate-new-buffer \"*capture*\")) (delete-other-windows) (add-hook 'org-select-mode-hook 'delete-other-windows) (org-roam-capture nil))"
